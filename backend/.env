# Environment variables for backend AdWall
# Copy this file to .env (already placed) and replace placeholder values with real secrets.

## App
NODE_ENV=development
PORT=8000

## Base URL used when generating public image links
BASE_URL=http://localhost:8000

## Database (MongoDB)
# Examples:
#  - local: mongodb://localhost:27017/addwall
#  - Atlas: mongodb+srv://<user>:<pass>@cluster0.mongodb.net/addwall?retryWrites=true&w=majority
DB_URI=mongodb://localhost:27017/addwall

## JWT
JWT_SECRET_KEY=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=30d

## Email (used by utils/sendEmail.js)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=you@example.com
EMAIL_PASSWORD=your_email_password_here

